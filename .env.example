# ============================================
# XrayR 配置生成器 - 环境变量配置
# ============================================

# 服务器端口
PORT=61145

# JWT 密钥（重要：生产环境必须修改为强随机字符串）
# 建议使用以下命令生成：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 管理员密码（首次登录使用，登录后建议在系统设置中修改）
# 要求：至少 8 位，包含字母和数字
ADMIN_PASSWORD=admin123456

# 数据存储路径
DATA_PATH=./data

# CORS 允许的来源（生产环境建议设置具体域名）
# 示例：CORS_ORIGIN=https://your-domain.com
# 留空或不设置则允许所有来源（仅开发环境使用）
# CORS_ORIGIN=

# ============================================
# 生产环境安全检查清单
# ============================================
# 1. 修改 JWT_SECRET 为强随机字符串（至少 64 字符）
# 2. 修改 ADMIN_PASSWORD 为强密码
# 3. 设置 CORS_ORIGIN 为具体域名
# 4. 使用 HTTPS 反向代理（如 Nginx）
# 5. 配置防火墙，仅开放必要端口
# 6. 定期备份 data 目录
# ============================================
